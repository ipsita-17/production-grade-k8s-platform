apiVersion: v1
kind: Service

metadata:
  name: backend-service

spec:
  selector:
    app: backend

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

  type: {{ .Values.service.type }}